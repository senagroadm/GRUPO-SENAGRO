# Development Dockerfile for NEXUS ERP
FROM node:20-alpine
WORKDIR /app
RUN apk add --no-cache libc6-compat git bash

COPY package.json package-lock.json* bun.lock* ./
RUN npm install

COPY . .

ENV NODE_ENV=development
ENV PORT=3000
EXPOSE 3000

CMD ["npm", "run", "dev"]
